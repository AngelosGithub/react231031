import React from "react";

function App(props) {
  // HOOK : use로 시작하는 함수
  // hook은 컴포넌트 상단에 작성해야 함
  // 조건문이나 반복문 중첩된 함수 안에 작성할 수 없음
  // (if, for, function 내부에 작성 불가)
  return <div></div>;
}

export default App;
